const initState = {
    projects: [
        {id: "1", title: "title", content: "some content"},
        {id: "2", title: "title", content: "some content"},
        {id: "3", title: "title", content: "some content"}
    ]
}

const ProjectReducer = (state = initState, action) => {
    return state
}

export default ProjectReducer